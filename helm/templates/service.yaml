apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-example.fullname" . }}
  labels:
    {{- include "express-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express-example.selectorLabels" . | nindent 4 }}
